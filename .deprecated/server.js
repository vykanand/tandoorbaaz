// Deprecated entry point. Use `index.js` as the single server entry.
// Keep this file as a safeguard to avoid confusion if someone runs `node server.js`.
console.warn("[Deprecated] Please run the server via `npm start` or `node index.js`. server.js is no longer used.");
process.exit(0);
